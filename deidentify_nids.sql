-------------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------------
---------------------------------------- NID DEIDENTIFICATION -----------------------------------------
-------------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------------

-- Options
-- MD5()
-- SHA(), sha1(), sha2()


SELECT *, MD5(nid) as Hashed_NID
from romao_mozart_cohort_all;

SELECT *, SHA(nid) as Hashed_NID
from romao_mozart_cohort_all;



